#ifndef ___UI_H___
#define ___UI_H___

#include <stdio.h>
#include <string.h>
#include "general.h"

void print_info();

void input_line(char []);

void input(GRID, GRID);

void print_sol(GRID, GRID);

void print_err(GRID, BYTE);

#endif
